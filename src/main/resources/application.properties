spring.datasource.url= jdbc:mysql://localhost:3306/bdventas?useSSL=false
spring.datasource.username= root
spring.datasource.password= mysql

spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect

# Configuración para imágenes
app.image-directory=Images
app.allowed-image-extensions=png,jpg,jpeg

# Configuración para archivos Excel
app.excel-directory=Excels
app.allowed-excel-extensions=xlsx,xls

app.allowed-extensions=png

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB